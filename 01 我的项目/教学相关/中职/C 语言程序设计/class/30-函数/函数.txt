1.#include <stdio.h>
fun(int i,int j)
{
    int x=7;
    printf("i=%d;j=%d;x=%d\n",i,j,x);
}
main()
{
    int i=2,x=5,j=7;
    fun(j,6);
    printf("i=%d;j=%d;x=%d\n",i,j,x);
}
2.
#include <stdio.h>
func(int a,int b)
{
    int c;
    c=a+b;
    return c;
}
void main()
{
    int x=6,y=7,z=8,r;
    r=func((x--,y++,x+y),z--);
    printf("%d\n",r); 
}
3.#include <stdio.h>
prn(int a,int b,int c,int max,int min)
{
    max=(max=a>b?a:b)>c?max:c;
    min=(min=a<b?a:b)<c?min:c;
    printf("max=%d  min=%d\n",max,min);
}
void main()
{
    int x,y;
    x=y=0;
    prn(19,23,-4,x,y);
    printf("max=%d  min=%d",x,y);
}
4.下面函数 func 的功能是 ________。
#include <stdio.h>
#include <math.h>
long func(long num)
{
    long k=1;
    num=abs(num);
    do {
        k *= num % 10;
        num /= 10;
    } while(num);
    return k;
}
main()
{
    long n;
    printf("\nPlease enter a number: ");
    scanf("%ld",&n);
    printf("\nThe product of its digits is %ld.",func(n));
}
5.#include <stdio.h>
int t(int x,int y)
{
    return (y-x)*x;
}
void main()
{
    int a=3,b=4,c=5,d;
    d=t(t(a,b),t(a,c));
    printf("%d\n",d);
}
6.#include <stdio.h>
void f(int a,int b)
{
    int t;
    t=a; a=b; b=t;
}
void main()
{
    int x=1,y=3,z=2;
    if(x>y) f(x,y);
    else if(y>z) f(x,z);
    else f(x,z);
    printf("%d,%d,%d\n",x,y,z);
}
7.
以下程序的功能是求任意两个整数 a 和 b 的最大公约数，并予以显示。请将程序补充完整。
#include <stdio.h>
int codivisor(int n1,int n2)/**********/
{
    int t;
    while (n2!=0) { 
	    //(1) ; 
		n1=n2; 
		n2=t; 
	}
    return /*(2)*/ ;
}
void main()
{
    int a,b,x;
    printf("please input two numbers:");
    scanf("%d%d",&a,&b);
    x=codivisor(a,b);
    printf("maximum common divisor of %d and %d is: %d\n",a,b,x);
}
8.以下程序的功能是计算函数 F(x,y,z) = (x+z)/(y-z) + (y+2z)/(y-2z) 的值，请将程序补充完整。
#include <stdio.h>
float f(float x,float y)
{
    float value;
    value=/*1*/;
    return value;
}
main()
{
    float x,y,z;
    scanf("%f%f%f",&x,&y,&z);
    sum=f(x+z,y-z)+f(/*2*/);
    printf("sum=%f\n",sum);
}
9.以下程序是将输入的一个整数反序并输出来，例如输入 1234，则输出 4321，输入 -1234，则输出 -4321，请将程序补充完整。
#include <stdio.h>
void printp(long int n)
{
    int i=0;
    if(n==0) return;
    else
     while(n)
     {
//        if(/*1*/)  printf("%ld",n%10);
        else printf("%ld",-n%10);
        i++;
        /*2*/
     }
}
void main()
{
    long int n;
    scanf("%ld",&n);
    printp(n);
    printf("\n");
}
三、程序改错题（20 分）
主函数 main() 调用 fun() 函数，其中 fun 函数的功能是：找出 100 到 999 之间（含 100 和 999）所有各位上数字之和为 x（x 为正整数）
的整数并输出，符合条件的整数个数作为函数值返回。例如，当 x 值为 5 时，
满足条件的整数有：104, 113, 122, 131, 140, 203, 212, 220, 230, 302, 311, 320, 401, 410, 500，一共 15 个。
#include<stdio.h>
void fun(int x)
{
    int n,s1,s2,s3,t;
    n=0;
    t=100;
    while(t<=999)
    {
        s1=t%10; s2=t%10%10; s3=t/100;
        if(s1+s2+s3==x)
        {
            printf("%5d",t);
            n++;
        }
        t++;
    }
    return n;
}
int main()
{
    int x=-1,y;
    while(x<=0)
    {
        printf("Please input x(x>0):");
        scanf("%d",&x);
    }
    fun(x);
    printf("\nThe result is: %d\n",y);
}
------函数的嵌套调用-------------------------
1.#include <stdio.h>
#define K 4
#define N 5
long f1(int n,int k)
{
    long power=n;
    int i;
    for(i=1;i<=k;i++)
        power *= n;
    return power;
}
long f2(int n,int k)
{
    long sum=0;
    int i;
    for(i=1;i<=n;i++)
        sum += f1(i,k);
    return sum;
}
void main()
{
    printf("%d\n",f2(N,K));
}
2.#include <stdio.h>
long f1(int p)
{
    long r;
    long f2(int); 
    p = p * p;
    r = f2(p);
    return r;
    
}
long f2(int q)
{
    long c;
    int i;
    c = 1;
    for(i=1;i<=q;i++)
        c = c * i;
    return c;
}
void main()
{
    int i;
    long s=0;
    for(i=1;i<=2;i++)
        s = s + f1(i);
    printf("\ns=%ld\n",s);
}
3.下面程序用于计算 f(k,n) = 1^k + 2^k + ... + n^k，其中函数 power(m,n) 计算 m，
函数 sum(k,n) 计算f(k,n)。请将程序补充完整。
#include <stdio.h>
long sum(int k,int n);
void main()
{
    int k,n;
    scanf("%d%d",&n,&k);
    printf("f(%d,%d) = %ld\n",k,n,sum(k,n));
}
long power(int m,int n)
{
    int i;
    /*1*/ ;
    for(i=1;i<=n;i++)
     /*2*/;
    return p;
}
long sum(int k,int n){
	int i;
	/*3*/
	for(i=1;i<=n;i++)
	  /*4*/
	return s;
}
4.#include <stdio.h>
ss2(int n)
{
    return (n+2);
}
ss1(int n)
{
    int m=10;
    m=n+ss2(m);
    return(m);
}
void main()
{
    int k=3;
    printf("%d",ss1(k));
}
5.
#include <stdio.h>
fac(int n)
{
    int i,s=1;
    for(i=1;i<=n;i++)
        s=s*i;
    return s;
}
sum(int n)
{
    int i,sum=0;
    for(i=1;i<=n;i++)
        sum=sum+fac(i);
    return sum;
}
void main()
{
    int n=5;
    printf("%d",sum(n));
}
6.程序填空题（共 8 分）
求 s= x/1! +x^2/2! +x^3/3! +......+x^n/n!
，n, x 由键盘输入。请将程序补充完整。
#include <stdio.h>
fac(int n)
{
    int i,s=1;
    for(i=1;i<=n;i++)
        s = s * i;
    return s;
}
float sum(int n,float x)
{
    int i;
    float sum=0,p=1;
    for(i=1;i<=n;i++)
    {
        p = p * x;
        /*1*/ ;
    }
    return sum;
}
void main()
{
    int n;
    float x;
    scanf("%d%f",&n,&x);
    printf("%f",/*2*/);
}
7.(1!)^2 +(2!)^2 +（3!)^2+(4!)^2;
#include <stdio.h>
long f1(int p)
{
}
long f2(int q)
{
}
void main()
{
    int i;
    long s=0;
    for(i=1;i<=4;i++)
        s = s + f1(i);
    printf("s= %ld",s);
}

-----递归调用--------
1.#include <stdio.h>
int f(int x)
{
    int y;
    if(x==0||x==1) return (3);
    y=x * x - f(x-2);
    return y;
}
void main()
{
    int z;
    z=f(3);
    printf("%d\n",z);
}
2.#include <stdio.h>
f(int x)
{
    if ( x == 0 ) return 0 ;
    else return (x%10+f(x/10) );
}
void main()
{
    printf("%d,%d",f(0),f(267));
}
3.#include <stdio.h>
long fun(int n)
{
    long s;
    if(n==1 || n==2)
        s=2;
    else
        s=n-fun(n-1);
    return s;
}
void main()
{
    printf("%ld\n",fun(5));
}
4.#include <stdio.h>
int ab(int x,int y);
main()
{
    int a=90,b=36;
    ab(a,b);
    printf("a=%d,b=%d",a,b);
}
int ab(int x,int y)
{
    if(y==0)
        printf("x=%d\n",x);
    else
        printf("x=%d,y=%d\n",x,y);
    return ab(y,x%y);
}
5.#include <stdio.h>
int sum(int a,int b)
{
    if(b==a) return a;
    return a+sum(a+1,b);
}
void main()
{
   printf("%d",sum(1,5));
}
6.
#include <stdio.h>
int f(int num)
{
    if(num==1)
    {
        printf("%d",num);
        return 0;
    }
    f(num/2);
    printf("%d",num%2);
}
void main()
{
    int num;
    printf("please input the num:");
    num=18;
    f(num);
    printf("\n");
}
7.#include <stdio.h>
int f(int n)
{
    if(n) return f(n-1)+n;
    else return n;
}
void main()
{
    int n=4;
    printf("%d",f(n));
}
8.#include <stdio.h>
int fun(int x)
{
    int s;
    if(x==1) s=1;
    else s=x*fun(x-2);
    printf("s=%d\n",s);
    return s;
}
main()
{
    int n=5;
    printf("\n%d",fun(n));
}
9.程序设计题
Fib(n) = 
   0    (n=0)
   1    (n=1)
   Fib(n-1)+Fib(n-2) (其他)
