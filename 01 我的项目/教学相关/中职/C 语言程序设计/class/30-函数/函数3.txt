1.#include <stdio.h>
int f(int x)
{
    int y;
    if(x==0||x==1) return (3);
    y=x * x - f(x-2);
    return y;
}
void main()
{
    int z;
    z=f(3);
    printf("%d\n",z);
}
2.#include <stdio.h>
f(int x)
{
    if ( x == 0 ) return 0 ;
    else return (x%10+f(x/10) );
}
void main()
{
    printf("%d,%d",f(0),f(267));
}
3.#include <stdio.h>
long fun(int n)
{
    long s;
    if(n==1 || n==2)
        s=2;
    else
        s=n-fun(n-1);
    return s;
}
void main()
{
    printf("%ld\n",fun(5));
}
4.#include <stdio.h>
int ab(int x,int y);
main()
{
    int a=90,b=36;
    ab(a,b);
    printf("a=%d,b=%d",a,b);
}
int ab(int x,int y)
{
    if(y==0)
        printf("x=%d\n",x);
    else
        printf("x=%d,y=%d\n",x,y);
    return ab(y,x%y);
}
5.#include <stdio.h>
int sum(int a,int b)
{
    if(b==a) return a;
    return a+sum(a+1,b);
}
void main()
{
   printf("%d",sum(1,5));
}
6.
#include <stdio.h>
int f(int num)
{
    if(num==1)
    {
        printf("%d",num);
        return 0;
    }
    f(num/2);
    printf("%d",num%2);
}
void main()
{
    int num;
    printf("please input the num:");
    num=18;
    f(num);
    printf("\n");
}
7.#include <stdio.h>
int f(int n)
{
    if(n) return f(n-1)+n;
    else return n;
}
void main()
{
    int n=4;
    printf("%d",f(n));
}
8.#include <stdio.h>
int fun(int x)
{
    int s;
    if(x==1) s=1;
    else s=x*fun(x-2);
    printf("s=%d\n",s);
    return s;
}
main()
{
    int n=5;
    printf("\n%d",fun(n));
}
9.程序设计题
Fib(n) = 
   0    (n=0)
   1    (n=1)
   Fib(n-1)+Fib(n-2) (其他)