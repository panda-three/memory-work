一、程序阅读题（每题5分，共10分）
1.
   #include <stdio.h>
   void ss(int a[])
   {
       int i, j, t;
       for(i=0; i<4; i++)
           for(j=0; j<4-i; j++)
               if(a[j] > a[j+1])
               {
                   t = a[j]; a[j] = a[j+1]; a[j+1] = t;
               }
   }
   void main()
   {
       int a[5] = {5, 2, 1, 4, 3}, i, t, j;
       ss(a);
       for(i=0; i<=4; i++)
           printf("%d ", a[i]);
   }

2.
   #include <stdio.h>
   void f(int a[])
   {
       int i=0;
       while(a[i]<=10)
       {
           printf("%4d", a[i]);
           i++;
       }
   }
   void main()
   {
       int a[] = {1, 5, 10, 9, 11, 7};
       f(a+1);
   }
二、程序填空题（每空3分，共21分）
1. 已定义一个含有30个元素的数组s，函数fav1的功能是按顺序分别赋予各元素从2开始的偶数，函数fav2则按顺序每五个元素求一个平均值，并将该值存放在数组w中。请将程序填充完整。
   #include <stdio.h>
   #define SIZE 30
   void fav1(int s[])
   {
       int k, i;
       for(k=2; i=0; i<SIZE; i++) // (1)
           s[i] = k;
           k += 2;
   }
   void fav2(int s[], float w[])
   {
       float sum;
       int k, i;
       sum = 0.0;
       for(k=0, i=0; i<SIZE; i++)
       {
           sum += s[i];
           if((i+1)%5 == 0)
           {
               w[k] = sum / 5; // (2)
               k++;
           }
       }
   }
   void main()
   {
       int s[SIZE];
       int i;
       float w[SIZE/5];
       fav1(s);
       for(i=0; i<SIZE; i++)
           printf("%4d", s[i]);
       printf("\n");
       fav2(s, w);
       for(i=0; i<SIZE/5; i++)
           printf("%4.1f", w[i]);
   }

2. 以下search函数的功能是利用顺序查找法从数组a的10个元素中对关键字m进行查找。若找到，返回此元素的下标；若仍未找到，则返回值-1。请填空。
   #include <stdio.h>
   int search(int a[], int m)
   {
       int i;
       for(i=0; i<10; i++) // (3)
           if(a[i] == m) return i; // (4)
       return -1; // (5)
   }
for(i=0; i<=9; i++)
    if( (1) ) return(1);
return(-1);
}
main()
{
    int a[10], m, i, no;
    for(i=0; i<10; i++)
        scanf("%d", &a[i]);
    scanf("%d", &m);
    no = search( (2) );
    if( (3) )
        printf("\nOK FOUND!%d", no+1);
    else
        printf("\nSorry Not Found! ");
}

三、程序改错题（每空4分，共16分）
函数fun()的功能是：用选择法对数组中的n个元素按从大到小的顺序进行排序。下面给定的程序存在错误，请改正。
#include <stdio.h>
#define N 20
/* ********FOUND(1)******** */
void fun(int n, int a[])
{
    int i, j, t, p;
    /* ********FOUND(2)******** */
    for(j=0; j<n; j++)
    {
        p = j;
        /* ********FOUND(3)******** */
        for(i=1; i<n; i++)
            if(a[i] > a[p])
                p = i;
        t = a[p];
        a[p] = a[j];
        /* ********FOUND(4)******** */
        a[p] = t;
    }
}
main()
{
    int a[N] = {11, 32, -5, 2, 14}, i, m = 5;
    printf("排序前的数据：");
    for(i=0; i<m; i++)
        printf("%d ", a[i]);
    printf("\n");
    fun(a, m);
    printf("排序后的数据：");
    for(i=0; i<m; i++)
        printf("%d ", a[i]);
    printf("\n");
}

【课后练】（满分50分）

一、程序阅读题（共5分）
#include <stdio.h>
void swap1(int c[])
{
    int t;
    t = c[0]; c[0] = c[1]; c[1] = t;
}
void swap2(int c0, int c1)
{
    int t;
    t = c0; c0 = c1; c1 = t;
}
void main()
{
    int a[2] = {4, 7}, b[2] = {4, 7};
    swap1(a);
    swap2(b[0], b[1]);
    printf("%d, %d, %d, %d\n", a[1], b[0], b[1]);
}

二、程序填空题（每空3分，共21分）
1. 下面的程序用来将一个十进制正整数转化成八进制数，例如输入一个正整数25，则输出31，请将程序填充完整。
#include <stdio.h>
void sub(int c, int d[])
{
    int e, i = 9;
    while(c != 0)
    {
        e = c % 8;
        d[i] = e;
        (1) ;
        i--;
    }
}
void main()
{
    int c;
    printf("请输入一个正整数：");
    scanf("%d", &c);
    int d[10] = {0};
    printf("八进制数为：");
    for(int i = 9; i >= 0; i--)
        printf("%d", d[i]);
    int i=0, j=0, a, b[10] = {0};
printf("Please input a integer: ");
scanf("%d", &a);
(2) ;
for(i=0; i<10; i++)
{
    if( (3) ) j++;
    if(j != 0) printf("%d", b[i]);
}
2. 函数biseach的作用是应用折半查找法从存有N个整数的升序数组a中对关键字key进行查找。请将程序补充完整。
#include <stdio.h>
#define N 15
biseach(int a[N], int key)
{
    int low=0, high=N-1, mid;
    while( (1) )
    {
        mid = (low + high) / 2;
        if(key < a[mid])
            high = mid - 1;
        else if(key > a[mid])
            (2) ;
        else
            return mid;
    }
    return (3);
}
void main()
{
    int b[N], n, i, j;
    printf("Please input a %d element increasing sequence: ", N);
    for(i=0; i<N; i++)
    {
        printf("b[%d] = ", i);
        scanf("%d", &b);
    }
    printf("Please input a searching key: ");
    scanf("%d", &n);
    (4) ;
    if(j == -1)
        printf("Don't find %d\n", n);
    else
        printf("b[%d] = %d\n", j, n);
}
}
三、程序改错题（每空3分，共12分）
程序去除主函数main()中字符串str中的非字母字符。函数IsAlpha()的功能是判断字符c是否为字母。下面给定的程序存在错误，请改正。
#include <stdio.h>
#include <conio.h>
int IsAlpha(char c)
/* ********FOUND(1)******** */
return ((c>='a' && c<='z') && (c>='A' && c<='Z'));
}
int main()
{
    char str[] = "/* * One World, One Dream! * */";
    int prev, next;
    prev = next = 0;
    while(str[next])
    {
        /* ********FOUND(2)******** */
        if(IsAlpha(str))
        {
            /* ********FOUND(3)******** */
            str[next] = str[prev];
            prev++;
        }
        next++;
    }
    /* ********FOUND(4)******** */
    str[next] = 0;
    puts(str);
}
四、程序设计题（共12分）
从键盘上输入10个整数，要求编写求出其中两个大数的最大公约数。该程序先将它们按降序排序，然后求出两个大数的最大公约数。
(1)编写void sort(int n, int k)函数，对数组n中的k个元素进行降序排序。
(2)编写int gcd(int a, int b)函数，求参数a, b的最大公约数。
#include <stdio.h>
#include <stdlib.h>
void main()
{
    void sort(int n[], int k);
    int gcd(int a, int b);
    int n[10], i, gbs;
    printf("请输入10个整数：");
    for(i=0; i<10; i++)
        scanf("%d", &n[i]);
    sort(n, 10);
    gbs = gcd(n[0], n[1]);
    printf("\n%d, %d's gcd = %d\n", n[0], n[1], gbs);
}
void sort(int n[], int k)
{
    // 排序函数实现
}
int gcd(int a, int b)
{
    // 最大公约数函数实现
}