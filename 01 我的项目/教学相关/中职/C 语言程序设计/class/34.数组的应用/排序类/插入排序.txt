 第九节 数组的应用——插入排序（满分20分）

【课堂练习】

一、简答题（共10分，共10分）

1. 写出插入排序算法的基本思想（2分）
   首先对前两个元素进行比较，按排序要求排列好，然后将第3个元素与前两个已排好的元素做比较，按排序要求找到其相应的位置，
最后通过交换将其插入该位置上。对第4个及以后的元素也依次进行这样的插入，直到全部元素排好序为止。

2. 数组中有6个元素，值分别为10, 9, 11, 8, 20, 13，用插入排序法对数组进行升序排序。
   第一轮：a[1]插入a[0]之后，即不用变化
第二轮：a[2]插入a[1]之后，即不用变化
第三轮：a[3]=8 应插入a[0]处，a[2]~a[0]依次后移
第四轮：a[4]=20 不用变化
第五轮：a[5]插入a[3]处，a[4]、a[3]依次后移

二、程序设计题（共10分）

1. 从键盘输入8个整数给数组，将这8个整数用插入法进行升序排序。

【课后练】

一、程序填空题（共8分）

将8个两位整数数组中下标范围为[3,6]范围内的元素用插入法进行升序排序，并保持其他元素的顺序不变。

#include <stdio.h>
void main()
{
    int i,j,x;
    int a[8]={34,65,24,76,87,96,43,21};
    for(i=0;i<8;i++)
        printf("%d\t",a[i]);
    printf("\n");
    for(i=3;i<=6;i++)
    {
        _______ ;
        for(j=i-1; _______ ;j--)
        {
            if( _______ )
                a[j+1]=a[j];
            else
                break;
        }
        _______ ;
    }
    printf("排序后：");
    for(i=0;i<8;i++)
        printf("%d\t",a[i]);
    printf("\n");
}

二、程序改错题（共9分）

将N个数按升序进行排序，最后将排序的结果输出。下面给定的程序存在错误，请改正。
#include <stdio.h>
#define N 8
void main()
{
    int i,j;
    int temp;
    int R[] = {8,7,4,3,1,6,2,9};
    printf("排序前：\n");
    for(i=0;i<N;i++)
        printf("%d\t",R[i]);
    printf("\n");
    /********* FOUND(1) *********/
    for(i=1;i<=N;i++)
    {
        temp=R[i];
        j=i-1;
    /********* FOUND(2) *********/
    while(j>0 && (temp < R[j]))
    {
        R[j+1] = R[j];
        j--;
    }
    /********* FOUND(3) *********/
    R[j+1] = R[i];
   }
    printf("排序后：\n");
    for(i=0;i<N;i++)
        printf("%d\t",R[i]);
    printf("\n");
}
三、程序设计题（共13分）

从键盘输入一个4×4的矩阵，将矩阵中奇数行降序排序，偶数行升序排序