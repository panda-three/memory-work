第五节 数组的应用——最大值和最小值问题

【课堂练习】（满分24分）

一、简答题（共10分）
写出求最值问题的基本原理和基本程序段。

二、程序填空题（每空2分，共14分）
1. 下列程序的功能是：打印一个5行4列的矩阵，并将每行中的最小数所在的位置找出存放在b数组中。矩阵的元素值由键盘输入。请仔细阅读程序并在程序空白处填入所需的内容。
#include<stdio.h>
main()
{
    int i,j,t;
    int j1,min,a[5][4],b[5];
    t=1;
    for(i=0;i<5;i++)
    {
        for(j=0;j<4;j++)
        {
            ______(1)______;
            printf("%4d",a[i][j]);
        }
        printf("\n");
    }
    printf("\n");
    for(i=0;i<5;i++)
    {
        min=a[i][0];
        ______(1)______;
        for(j=1;j<4;j++)
	        if(a[i][j]<min)
	        {
	            min=a[i][j];j1=______(3)______;
	        }
	        b[i]=______(4)_____;
	        printf("%d,%d,%d\n",b[i],i,j1);
    
    }
}


2.从键盘输入8个字符串，且每个字符串的长度都不超过20，找出这8个字符串中的最大字符串及其所在位置。请将程序填充完整。
#include<stdio.h>
#include<string.h>
void main()
{
    int i,n,pos;
    char a[8][20],max[20],min[20];
    for(i=0;i<8;i++)
        gets(a[i]);
    ________(1)________;
    pos=0;
    for(i=1;i<8;i++)
        if(________(2)________)
        {
            strcpy(max,a[i]);
            ________(3)________;
        }
    printf("max=%s,pos=%d",max,pos);
}

一、程序阅读题（每题6分，共12分）

1. #include<stdio.h>
void main()
{
    int min1,min2,i;
    int a[10]={43,56,35,12,45,23,86,22,86,98};
    min1=min2=100;
    for(i=0;i<10;i++)
    {
        printf("%d,",a[i]);
        if(a[i]<min1)
        {
            min2=min1;
            min1=a[i];
        }
        else if(a[i]<min2)
            min2=a[i];
    }
    printf("\nmin1= %d,min2= %d\n",min1,min2);
}

2. #include<stdio.h>
void main()
{
    int i,j,row,column,m;
    int array[3][3]={{100,200,300},{28,72,-30},{-850,2,6}};
    m=array[0][0];
    for(i=0;i<3;i++)
        for(j=0;j<3;j++)
            if(array[i][j]<m)
                {m=array[i][j]; row=i; column=j;}
    printf("%d,%d,%d",m,row,column);
}
二、程序填空题（每空2分，共14分）
1. 本程序的功能是从键盘上输入10个整数，将最小值元素与第1个交换，不改变数组中的其他元素。
#include <stdio.h>
void main()
{
    int a[10];
    int i,p,t;
    for(i=0;i<10;i++)
        scanf("%d",___(1)___);
    p=0;
    for(i=1;i<10;i++)
        if(___(2)___)
            p=i;
    if(___(3)___)
    {
        t=a[0];
        a[0]=a[p];
        a[p]=t;
    }
    for(i=0;i<10;i++)
        printf("%5d",a[i]);
}

2. 本程序的功能是求一个二维数组中每行的最大值和每行的和。
#include <stdio.h>
main()
{
    int a[5][5],b[5],c[5],i,j,k,s;
    for(i=0;i<5;i++)
        for(j=0;j<5;j++)
            scanf("%d",&a[i][j]);
    for(i=0;i<5;i++)
    {
        k=a[i][0];
        for(j=0;j<5;j++)
        {
            if(k<a[i][j])
                ___(2)___;
            s=s+a[i][j];
        }
        b[i]=k;
        ___(3)___;
    }
    for(i=0;i<5;i++)
    {
        for(j=0;j<5;j++)
            printf("%5d",___(4)___);
        printf("%5d%5d",b[i],c[i]);
        printf("\n");
    }
}