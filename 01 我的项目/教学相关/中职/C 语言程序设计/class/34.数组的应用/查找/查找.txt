第十节 数组的应用——查找（满分40分）

【课堂练习】

一、简答题（每题8分，共16分）

1. 顺序查找的原理是什么？适用什么范围？
2. 折半查找的原理是什么？适用什么范围？

二、程序填空题（共12分）
由键盘输入一个待查的整数n，若n存在，则输出它在数组中第一个出现的位置，否则给出没找到信息。请将程序填空完整。
#include <stdio.h>
#define N 10
void main()
{
    int i,n;
    int a[10]={25,2,67,34,65,86,56,78,24,34};
    _______(1)_______
    i=0;
    while(a[i]!=n && _______(2)_______)
        i=_______(3)_______;
    if(_______(4)_______)
        printf("%d not found!",n);
    else
        printf("%d in position %d\n",n,i);
}

三、程序改错题（共12分）
从键盘输入一个数，用折半查找这个数在数组中的位置。下面给定的程序存在错误，请改正。
#include <stdio.h>
#define N 10
void main()
{
    int i,a[N]={-3,4,7,9,13,45,67,78,89,148};
    int left,right,mid,k,m;
    printf("a数组中的数据如下：");
    for(i=0;i<N;i++)
        printf("%d ",a[i]);
    printf("\nEnter m: ");
    scanf("%d",&m);
    /*  Found(1)  */
    left=1,right=N;
    k=-1;
    while(left<=right)
    {
        mid=(left+right)/2;
        if(m>a[mid])
            /*  Found(2)  */
            right=mid-1;
        else if(m<a[mid])
         /*  Found(3)  */
            left=mid+1;
        else {k=1;break;}
    }
    if(k>=0)
        printf("m=%d,index=%d\n",m,mid);
    else
        printf("Not be found!\n");
}

【课后练】
一、程序填空题（共12分）
从键盘输入10个互不相同的整数，然后对这些数进行排序，由键盘输入一个数，用折半查找这个数在数组中的位置。请将程序填空完整。
#include <stdio.h>
void main()
{
    int i,j,t,a[10],k,low,high,mid,n;
    for(i=0;i<10;i++)
        scanf("%d",&a[i]);
    for(i=0;i<=8;i++)
	{
		k=i;
    	for(j=i+1;j<=9;j++)
            if(a[k]>a[j]) _____(1)______ ;
            if(k!=i) {t=a[i];a[i]=a[k];a[k]=t;}
	} 
    for(i=0;i<10;i++)
        printf("%d\t",a[i]);
    low=0;high=9;k=0;
    scanf("%d",&n);
    while (k==0 && low<=high)
    {
        mid=(low+high)/2;
        if(n<a[mid]) _____(2)______ ;
        if(n>a[mid]) _____(3)______ ;
        if(n==a[mid]) k=1;
    }
    if(_____(4)______ )
        printf("%d in position %d\n",n,mid);
    else
        printf("Not be found!\n");
}


二、程序改错(共10分)
判断字符ch是否与str所指串中的某个字符串相同，若相同，什么也不做，若不同，则将其插在串的最后。
#include <string.h>
main()
{
    char str[81], c;
    int i;
    printf("\nPlease enter a string:");
    gets(str);
    printf("\nPlease enter the character to search:");
    /* found 1 */
    getchar(c);
    i = 0;
    while (str[i] || str[i]!= c)
      i++; 
    /* found 2 */
    if (str[i] == c){
    	  str[i]=c;
    	  /* found 3 */
    	  str[i]='0';
	}
    printf("\nThe result is %s\n", str);
}

三、程序设计题(第1题9分，第2题10分，共19分)
1. 从键盘输入10个互不相同的整数，再从键盘输入一个待查的数，若该数存在，则输出它在数组中最后一个出现的位置，否则给出没找到信息。
2. 从键盘输入10个整数，使用插入法排序后，然后往其中插入一个数，使其仍有序。若该数已在数组中，则输出它在数组中最后一个出现的位置，否则给出没找到信息。