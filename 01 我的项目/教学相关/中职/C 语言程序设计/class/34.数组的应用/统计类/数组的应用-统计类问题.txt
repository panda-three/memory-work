一、程序阅读题（每题3分，共6分）
1.#include<stdio.h>
void main()
{
	int x1, x2, x3, i;
	int a[] = {-5, 3, 6, 0, -5, 7, 2, 8, 0, 3};
	x1 = x2 = x3 = 0;
	for(i = 0; i < 10; i++)
		if(a[i] < 0) x1++;
		else if(a[i] == 0) x2++;
		else if(a[i] > 0) x3++;
	printf("%d %d %d\n", x1, x2, x3);
}
2.如图输入下列数据，写出下列程序的输出结果。（核对）
2 3 1 2 3 3 2 4 2 2 3 1 0
#include<stdio.h>
void main()
{
	int a[5], n, i;
	for(i = 1; i <= 4; i++)
	a[i] = 0;
	scanf("%d", &n);
	while(n > 0)
	{
		a[n]++;
		scanf("%d", &n);
	}
	for(i = 1; i <= 4; i++)
	printf("%d ", a[i]);
}
二、程序填空题（每空2分，共14分）
1.下列程序的功能是统计一个字符串中的字母、数字、空格和其它字符的个数。请将程序补充完整。
#include<stdio.h>
void main()
{
char arr[81];
int n[4] = {0};
printf("请输入一个字符串：");
gets(arr);
for(i = 0; arr[i] != '\0'; i++)
if( 1) n[0]++;
else if( 2) n[1]++;
else if( 3) n[2]++;
else n[3]++;
for(i = 0; i < 4; i++)
printf("%d ", n[i]);
}
2.从键盘输入20个整数，统计出其中素数的个数，并求出素数的平均值。
#include<stdio.h>
void main()
{
	int a[20], i, j, flag, sum, num ;
	(1)-------
	for(i = 0; i < 20; i++)
	scanf("%d", &a[i]);
	for(i = 0; i < 20; i++)
	{
	    (2)--------
		for(j = 2; j <= a[i] / 2; j++)
		if(3------)
		{
		    flag = 0;
		    break;
		}
		if(flag == 1){
	               sum += a[i];
		num++;
                                }
	}
	printf("素数个数为：%d\n", num);
	printf("平均值为：%.1f\n", (4------) );
}
课后练：
【课后练】
一、程序阅读题（每题4分，共8分）
1. #include <stdio.h>
   #include <string.h>
   void main()
   {
       int i, k, v[4] = {0};
       char s[] = "1424134211";
       for(k = 0; s[k]; k++)
       {
           switch(s[k])
           {
            case '1': i=0; break;
            case '2': i=1; break;
            case '3': i=2; break;
            case '4': i=3;
          }
        v[i]++;
       }
   for(k=0; k<4; k++)
    printf("%4d", v[k]);
}
2.#include <stdio.h>
void main()
{
    int i, x[10] = {1, 3, 1, 2, 0, 2, 3, 2, 0, 3}, n[4] = {0};
    for(i=0; i<10; i++)
        n[x[i]]++;
    for(i=0; i<4; i++)
        printf("%d ", n[i]);
}
二、程序填空题（每空2分，共14分）
1. 下列程序的功能是将n个学生的考试成绩进行分段统计，成绩由键盘输入存入数组a，各分段的人数存入b数组。
成绩为100的人数存到b中，成绩为90到99的人数存到b中，成绩为80到89的人数存到b中，
成绩为70到79的人数存到b中，成绩为60到69的人数存到b中，成绩为60分以下的人数存到b中。请将程序补充完整。
   #include<stdio.h>
   void main()
   {
       int i, n, a[200], b[6];
       scanf("%d", &n);
       for(i = 0; i < n; i++)
           scanf("%d", &a[i]);
       for(i = 0; i < 6; i++)
           b[i] = 0;
       for(i = 0; i < (1); i++)
           if(a[i]<60) b[0]++;
           else b[(2)]++;
       for(i = 0; i < 6; i++)
           printf("%d ", b[i]);
       printf("\n");
   }

2. 下列程序分类统计各元音字母（不区分大小写）和其他字符的个数。请将程序补充完整。
   #include<stdio.h>
   void main()
   {
       char str[81];
       int i, a[6];
       gets(str);
       for(i = 0; i < 6; i++)
           (1);
       i = 0;
       while(str[i])
       {
           switch(str[i])
           {
                case 'A':
                case 'a': a[0]++; break;
                case 'E':
                case 'e': a[1]++; break;
                case 'I':
                case 'i': a[2]++; break;
                case 'O':
                case 'o': a[3]++; break;
                case 'U':
                case 'u': a[4]++; break;
                default: (2) ;
           }
           (3)
       }
       for(i = 0; i < 6; i++)
           printf("%d ", a[i]);
       printf("\n");
   }

3. 有10名学生参加竞赛，a数组中存放学生性别，b数组中存放学生成绩。
   a[] = {'n', 'v', 'v', 'n', 'n', 'v',  'n', 'v', 'n', 'n'};
   b[] = {90, 92, 87, 78,91, 93, 94, 67, 88, 92, 80};
   其中n代表男生，v代表女生。若90分及以上能进入决赛，统计出进入决赛的男生及女生人数和没有进入决赛的男生人数。请将程序补充完整。
   #include<stdio.h>
   void main()
   {
       int i, x = 0, y = 0, z = 0;
       char a[] = {'n', 'v', 'v', 'n', 'v', 'v', 'n', 'v', 'v', 'n'};
       int b[] = {90, 92, 87, 85, 93, 94, 67, 88, 78, 93};
       for(i = 0; i < 10; i++)
           if( (1) )
               x++;
           else if( (2) )
               y++;
           else
               z++;
       printf("进入决赛的男生人数为: %d\n", x);
       printf("进入决赛的女生人数为: %d\n", y);
       printf("没有进入决赛的人数为: %d\n", z);
   }
三、程序改错题（共6分）
1.下列程序的功能是输入10个数，要求输出这10个数的平均值。
#include<stdio.h>
void main()
{
/* ********** FOUND1 ********** */
int score[10], i, aver, sum = 0;
printf("input 10 scores:\n");
for(i = 0; i < 10; i++)
/* ********** FOUND2 ********** */
scanf("%f", score);
printf("\n");
/* ********** FOUND3 ********** */
for(i = 1; i < =10; i++)
sum += score[i];
aver=sum/10.0;
printf("average is %5.2f\n", aver);
/**********  END  **********/
}
四、程序设计题（每题11分，共22分）
1.定义一个含有30个元素的数组，按顺序赋予从2开始的偶数，然后按顺序以每5个数一组的形式求出平均值并输出。
2.从键盘输入一串英文小写字母（不含其他字符），统计每个字母的数目，并输出字母及其相应的数目